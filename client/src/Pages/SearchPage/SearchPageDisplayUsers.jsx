// import SearchPage from './SearchPage'; // Adjust the path based on your project structure
// import { useGlobalSearchPage } from '../../context/SearchPageContext'

// const SearchPageDisplayUsers = () => {
//  const { searchResults, setSearchResults } = useGlobalSearchPage();
//   // Handle search results from SearchPage component
//   const handleSearchResult = (results) => {
//     setSearchResults(results);
//   };

//   return (
//     <div>
//       <SearchPage onSearchResult={handleSearchResult} />

//       <h3>Search Results</h3>
//       {searchResults.length > 0 ? (
//         <ul>
//           {searchResults.map((user) => (
//             <li key={user._id}>{user.name}</li>
//             // You can display other user details based on your schema
//           ))}
//         </ul>
//       ) : (
//         <p>No matching users found.</p>
//       )}
//     </div>
//   );
// };

// export default SearchPageDisplayUsers;




// import SearchPage from './SearchPage'; // Adjust the path based on your project structure
// import { useGlobalSearchPage } from '../../context/SearchPageContext'

// const SearchPageDisplayUsers = () => {
//  const { searchResults, setSearchResults } = useGlobalSearchPage();
//   // Handle search results from SearchPage component
//   const handleSearchResult = (results) => {
//     setSearchResults(results);
//   };

//   return (
//     <div>
//       <SearchPage onSearchResult={handleSearchResult} />

//       <h3>Search Results</h3>
//       {searchResults.length > 0 ? (
//         <ul>
//           {searchResults.map((user) => (
//             <li key={user._id}>{user.name}</li>
//             // You can display other user details based on your schema
//           ))}
//         </ul>
//       ) : (
//         <p>No matching users found.</p>
//       )}
//     </div>
//   );
// };

// export default SearchPageDisplayUsers;
